Algorithm for Extra Long Factorials:

Initialize result = 1 (BigInteger).

For each integer i from 2 up to n:

Multiply result *= i.

Print result.

This algorithm finds the factorial of n n, even for very large n n, by using a big-number variable type.
